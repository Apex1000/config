from django.urls import path, include
from uploader import views as uploader_views

urlpatterns = [
    # path("city/", uploader_views.CityUploader.as_view(), name="city"),
    # path("store/", uploader_views.StoreUploader.as_view(), name="city"),
]
